require 'spec_helper'

describe SmartbarsController do
  describe "GET root path" do
    before :each do
      #customer = Fabricate.build(:customer)
      #smartbars =
    end

    it "assigns @smartbars" do
      #get :index
      #assigns[:smartbars].should eq(:smartbars)
    end
  end
end